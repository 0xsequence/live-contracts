name: "proxy-hook"
version: "1"
description: "Immutable chains require proxies to have an implementation method, this hook implements it."

skip_networks: [1]

actions:
  - name: "wallet-proxy-hook"
    template: "erc-2470"
    arguments:
      salt: "{{salt-zero}}"
      creationCode: "{{Contract(./artifacts/proxy-hook.json).creationCode}}"
    output: true
